CREATE TABLE student (
                         id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
                         firstname VARCHAR(100),
                         lastname VARCHAR(20),
                         age INT
);
